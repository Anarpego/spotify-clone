import { screen, render } from '@testing-library/react';
import { AuthContext } from '../../context/AuthContext';


describe('first', () => {


	test('should first', () => {


	});

	// test('should show chilfren if is not logged', () => {

	// 	const contextValue = {
	// 		uid: 0,
	// 		checking: true,
	// 		logged: false,
	// 		name: '',
	// 		email: '',
	// 		gravatar: '',
	// 		register: (name: string, email: string, password: string, gravatar: string, card: object) => Promise<void>,


	// 	}

	// 	render(

	// 		<AuthContext.Provider value={contextValue}>

	// 		</AuthContext.Provider>
	// 	);


	// });

});